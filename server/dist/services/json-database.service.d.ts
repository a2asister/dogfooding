import { OnModuleInit } from '@nestjs/common';
import { CircuitBreakerConfig, CircuitBreakerState, RateLimitRule, RateLimitState, ChaosExperiment, IsolationConfig, IsolationState, MetricsSnapshot } from '../types';
export declare class JsonDatabaseService implements OnModuleInit {
    private readonly dbPath;
    private data;
    constructor();
    onModuleInit(): Promise<void>;
    private ensureDataDirectory;
    private getDefaultData;
    private loadData;
    private saveData;
    getCircuitConfigs(): Promise<CircuitBreakerConfig[]>;
    getCircuitConfig(id: string): Promise<CircuitBreakerConfig | undefined>;
    createCircuitConfig(config: Omit<CircuitBreakerConfig, 'id'>): Promise<CircuitBreakerConfig>;
    updateCircuitConfig(id: string, updates: Partial<CircuitBreakerConfig>): Promise<CircuitBreakerConfig | null>;
    deleteCircuitConfig(id: string): Promise<boolean>;
    getCircuitStates(): Promise<CircuitBreakerState[]>;
    getCircuitState(circuitId: string): Promise<CircuitBreakerState | undefined>;
    updateCircuitState(circuitId: string, updates: Partial<CircuitBreakerState>): Promise<CircuitBreakerState | null>;
    getRateLimitRules(): Promise<RateLimitRule[]>;
    getRateLimitRule(id: string): Promise<RateLimitRule | undefined>;
    createRateLimitRule(rule: Omit<RateLimitRule, 'id'>): Promise<RateLimitRule>;
    updateRateLimitRule(id: string, updates: Partial<RateLimitRule>): Promise<RateLimitRule | null>;
    deleteRateLimitRule(id: string): Promise<boolean>;
    getRateLimitStates(): Promise<RateLimitState[]>;
    getRateLimitState(ruleId: string): Promise<RateLimitState | undefined>;
    updateRateLimitState(ruleId: string, updates: Partial<RateLimitState>): Promise<RateLimitState | null>;
    getChaosExperiments(): Promise<ChaosExperiment[]>;
    getChaosExperiment(id: string): Promise<ChaosExperiment | undefined>;
    createChaosExperiment(experiment: Omit<ChaosExperiment, 'id' | 'startTime' | 'status'>): Promise<ChaosExperiment>;
    updateChaosExperiment(id: string, updates: Partial<ChaosExperiment>): Promise<ChaosExperiment | null>;
    deleteChaosExperiment(id: string): Promise<boolean>;
    getIsolationConfigs(): Promise<IsolationConfig[]>;
    getIsolationConfig(id: string): Promise<IsolationConfig | undefined>;
    createIsolationConfig(config: Omit<IsolationConfig, 'id'>): Promise<IsolationConfig>;
    updateIsolationConfig(id: string, updates: Partial<IsolationConfig>): Promise<IsolationConfig | null>;
    deleteIsolationConfig(id: string): Promise<boolean>;
    getIsolationStates(): Promise<IsolationState[]>;
    getIsolationState(configId: string): Promise<IsolationState | undefined>;
    updateIsolationState(configId: string, updates: Partial<IsolationState>): Promise<IsolationState | null>;
    addMetricsSnapshot(metrics: MetricsSnapshot): Promise<void>;
    getMetricsHistory(startTime?: number, endTime?: number): Promise<MetricsSnapshot[]>;
}
